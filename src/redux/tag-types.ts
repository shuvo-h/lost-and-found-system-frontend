export enum tagTypes {
    user = 'user',
    category = 'category',
    claim = 'claim',
    found = 'found',
}
 
 export const tagTypesList = [
    tagTypes.user,
    tagTypes.category,
    tagTypes.claim,
    tagTypes.found,
];