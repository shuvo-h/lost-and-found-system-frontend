export enum tagTypes {
    user = 'user',
    category = 'category',
    claim = 'claim',
    found = 'found',
    lost = 'lost',
    me = 'me',
    metric = 'metric',
    single_found = 'single_found',
    single_lost = 'single_lost',
}
 
 export const tagTypesList = [
    tagTypes.user,
    tagTypes.category,
    tagTypes.claim,
    tagTypes.found,
    tagTypes.lost,
    tagTypes.me,
    tagTypes.metric,
    tagTypes.single_found,
    tagTypes.single_lost,
];